rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read access to uploaded assets (logos, portfolio images, etc.)
    // Allow write/upload/delete access only to authenticated admin users
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
